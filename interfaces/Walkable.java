package interfaces;

/**
 * Interface representing walkable behavior.
 */
public interface Walkable {
    /**
     * Makes the animal walk a specified distance.
     * @param distance the distance for the animal to walk
     */
    void walk(int distance);
}